{
  "name": "firebase-app-monorepo",
  "private": true,
  "workspaces": [
    "apps/web-app",
    "apps/api-server"
  ],
  "scripts": {
    "dev:client": "npm run dev -w web-app",
    "dev:server": "npm run dev -w api-server",
    "dev": "npm run dev:server",
    "build:client": "npm run build -w web-app"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.97.0"
  }
}
